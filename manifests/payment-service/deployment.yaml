apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: microsvc
  labels:
    app: payment-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
      annotations:
        sidecar.istio.io/inject: "true"
    spec:
      initContainers:
        - name: wait-for-deps
          image: busybox
          command:
            - sh
            - -c
            - |
              until nc -z kafka 29092 && nc -z zookeeper 2181; do
                echo "waiting for kafka and zookeeper";
                sleep 3;
              done
      containers:
        - name: payment-service
          image: hafsa22/adv_microsvc:payment-service
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8002
          env:
            - name: REDIS_URL
              valueFrom:
                configMapKeyRef:
                  name: microsvc-config
                  key: REDIS_URL_PAYMENT
            - name: KAFKA_BOOTSTRAP
              valueFrom:
                configMapKeyRef:
                  name: microsvc-config
                  key: KAFKA_BOOTSTRAP
            - name: PYTHONUNBUFFERED
              value: "1"
          livenessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 60
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 15
            periodSeconds: 5
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - payment-service
                topologyKey: "kubernetes.io/hostname"
